@model  IEnumerable<aspnet.Models.PostModel>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>


<ul>
    @foreach (var post in Model)
    {
        <li>
            @Html.Raw(post.Text)
        </li>
    }
</ul>
